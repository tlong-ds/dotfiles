# open app
alt - return : pgrep -x ghostty > /dev/null && osascript -e 'tell application "Ghostty" to new tab' || open -a Ghostty
alt - b: open -a "Google Chrome"

alt - c: open -a "Zed" 
alt - x: open -a "Xcode"
